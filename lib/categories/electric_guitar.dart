import 'package:flutter/material.dart';

class ElectricGuitarPage extends StatelessWidget {
  const ElectricGuitarPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: null,
    );
  }
}
