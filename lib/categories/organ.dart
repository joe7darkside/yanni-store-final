import 'package:flutter/material.dart';

class OrganPage extends StatelessWidget {
  const OrganPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: null,
    );
  }
}