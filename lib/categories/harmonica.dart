import 'package:flutter/material.dart';

class HarmonicaPage extends StatelessWidget {
  const HarmonicaPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: null,
    );
  }
}