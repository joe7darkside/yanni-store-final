import 'package:flutter/material.dart';

class DrumsPage extends StatelessWidget {
  const DrumsPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: null,
    );
  }
}