import 'package:flutter/material.dart';

class MilodicaPage extends StatelessWidget {
  const MilodicaPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: null,
    );
  }
}