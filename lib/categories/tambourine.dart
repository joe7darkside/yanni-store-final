import 'package:flutter/material.dart';

class TambourinePage extends StatelessWidget {
  const TambourinePage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: null,
    );
  }
}