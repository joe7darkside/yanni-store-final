import 'package:flutter/material.dart';

class TrumpetPage extends StatelessWidget {
  const TrumpetPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: null,
    );
  }
}