import 'package:flutter/material.dart';

class ClarinetPage extends StatelessWidget {
  const ClarinetPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: null,
    );
  }
}