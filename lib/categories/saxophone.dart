import 'package:flutter/material.dart';

class SaxophonePage extends StatelessWidget {
  const SaxophonePage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: null,
    );
  }
}