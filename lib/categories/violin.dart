import 'package:flutter/material.dart';

class ViolinPage extends StatelessWidget {
  const ViolinPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: null,
    );
  }
}