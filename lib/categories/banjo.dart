import 'package:flutter/material.dart';

class BanjoPage extends StatelessWidget {
  const BanjoPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: null,
    );
  }
}