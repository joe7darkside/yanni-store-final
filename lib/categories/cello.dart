import 'package:flutter/material.dart';

class CelloPage extends StatelessWidget {
  const CelloPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: null,
    );
  }
}